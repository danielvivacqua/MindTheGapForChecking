@model IEnumerable<MindTheGap.Models.GapsTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GapEnd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GapEnd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GapStart }) |
            @Html.ActionLink("Details", "Details", new { id=item.GapStart }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GapStart })
        </td>
    </tr>
}

</table>
